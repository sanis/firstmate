Claude Code PreToolUse hook, driven with the real harness payload shape:
  {"hook_event_name":"PreToolUse","tool_name":"Bash","tool_input":{"command":"..."}}
piped to bin/fm-cd-pretool-check.sh --claude inside a primary firstmate checkout.
firstmate home = /private/tmp/fm-cd-ev/after (shown below as <home>)

$ cd <home> && tasks-axi list
BEFORE (790e925, default branch)
  DENY  (exit 2)
  [persistent-cd] a persistent top-level directory change in the primary firstmate checkout is blocked; it would move the shell out of the home so a later firstmate-owned command runs inside a project clone. Reach the target without moving the shell - use git -C <dir> or an absolute path on the command itself - or scope the cd to a subshell like (cd <dir> && ...).
AFTER  (f8ccf2d, this change)
  ALLOW (exit 0, no hook output)

$ cd <home>/projects/ansible
BEFORE (790e925, default branch)
  DENY  (exit 2)
  [persistent-cd] a persistent top-level directory change in the primary firstmate checkout is blocked; it would move the shell out of the home so a later firstmate-owned command runs inside a project clone. Reach the target without moving the shell - use git -C <dir> or an absolute path on the command itself - or scope the cd to a subshell like (cd <dir> && ...).
AFTER  (f8ccf2d, this change)
  DENY  (exit 2)
  [persistent-cd] a persistent top-level directory change in the primary firstmate checkout is blocked; it would move the shell out of the home so a later firstmate-owned command runs inside a project clone. Reach the target without moving the shell - use git -C <dir> or an absolute path on the command itself - or scope the cd to a subshell like (cd <dir> && ...).

$ (cd <home> && tasks-axi list)
BEFORE (790e925, default branch)
  ALLOW (exit 0, no hook output)
AFTER  (f8ccf2d, this change)
  ALLOW (exit 0, no hook output)

