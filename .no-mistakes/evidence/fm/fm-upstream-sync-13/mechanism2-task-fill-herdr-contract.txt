=== mechanism 2: the documented {TASK} fill vs the Herdr contract ===
task body used for the fill: "Restart the herdr session for the export lane, then profile it and report the hot path"
(the body deliberately mentions herdr lifecycle verbs - exactly the text the gate exists to catch)

==============================================================================
AFTER  - this merge (bin/fm-brief.sh at 8619524)
==============================================================================
  ship scaffold, UNGUARDED (no --herdr-lab)
    {TASK} fill sites in the scaffold: 1
    after the 'line' fill: task body appears 1 time(s)
      unguarded HARD SAFETY GATE as the worker would read it:
        | # Herdr lifecycle declaration - NOT ENABLED
        | **HARD SAFETY GATE:** this scaffold cannot inspect the task text filled in above.
        | If the task will start, stop, delete, restart, profile, or otherwise drive Herdr lifecycle behavior, stop and regenerate the brief with `--herdr-lab` before dispatch.
        | Do not add Herdr lifecycle commands to this unguarded brief by hand.
    after the 'global' fill: task body appears 1 time(s)
      unguarded HARD SAFETY GATE as the worker would read it:
        | # Herdr lifecycle declaration - NOT ENABLED
        | **HARD SAFETY GATE:** this scaffold cannot inspect the task text filled in above.
        | If the task will start, stop, delete, restart, profile, or otherwise drive Herdr lifecycle behavior, stop and regenerate the brief with `--herdr-lab` before dispatch.
        | Do not add Herdr lifecycle commands to this unguarded brief by hand.

  ship scaffold, GUARDED (--herdr-lab)
    {TASK} fill sites in the scaffold: 1
    after the 'line' fill: task body appears 1 time(s)
      guarded Herdr lifecycle contract as the worker would read it:
        | # Herdr isolation - HARD SAFETY CONTRACT
        | This brief was explicitly scaffolded with `--herdr-lab` because the task will drive Herdr lifecycle behavior.
        | On Herdr 0.7.3 the API socket is not relocatable by `HERDR_CONFIG_PATH`, `XDG_CONFIG_HOME`, or `HOME`.
        | A named non-`default` session plus a trailing `--session <name>` on every call is the only viable local isolation.
        | 1. Set `HERDR_LAB_HELPER='/tmp/fm-brief-fill.NC3fKI/bin/fm-herdr-lab.sh'` and generate the session name with `HERDR_LAB_SESSION=$("$HERDR_LAB_HELPER" name ship-guarded)`.
        |    Install `trap '"$HERDR_LAB_HELPER" teardown "$HERDR_LAB_SESSION"' EXIT` before provisioning, then provision only with `"$HERDR_LAB_HELPER" provision "$HERDR_LAB_SESSION"`.
        | 2. Run every task-specific non-lifecycle Herdr command through `"$HERDR_LAB_HELPER" run "$HERDR_LAB_SESSION" <arguments...>`.
        |    The helper appends the required trailing `--session "$HERDR_LAB_SESSION"`; `HERDR_SESSION` alone is never accepted as isolation.
    after the 'global' fill: task body appears 1 time(s)
      guarded Herdr lifecycle contract as the worker would read it:
        | # Herdr isolation - HARD SAFETY CONTRACT
        | This brief was explicitly scaffolded with `--herdr-lab` because the task will drive Herdr lifecycle behavior.
        | On Herdr 0.7.3 the API socket is not relocatable by `HERDR_CONFIG_PATH`, `XDG_CONFIG_HOME`, or `HOME`.
        | A named non-`default` session plus a trailing `--session <name>` on every call is the only viable local isolation.
        | 1. Set `HERDR_LAB_HELPER='/tmp/fm-brief-fill.NC3fKI/bin/fm-herdr-lab.sh'` and generate the session name with `HERDR_LAB_SESSION=$("$HERDR_LAB_HELPER" name ship-guarded)`.
        |    Install `trap '"$HERDR_LAB_HELPER" teardown "$HERDR_LAB_SESSION"' EXIT` before provisioning, then provision only with `"$HERDR_LAB_HELPER" provision "$HERDR_LAB_SESSION"`.
        | 2. Run every task-specific non-lifecycle Herdr command through `"$HERDR_LAB_HELPER" run "$HERDR_LAB_SESSION" <arguments...>`.
        |    The helper appends the required trailing `--session "$HERDR_LAB_SESSION"`; `HERDR_SESSION` alone is never accepted as isolation.

  scout scaffold, UNGUARDED
    {TASK} fill sites in the scaffold: 1
    after the 'line' fill: task body appears 1 time(s)
      unguarded HARD SAFETY GATE as the worker would read it:
        | # Herdr lifecycle declaration - NOT ENABLED
        | **HARD SAFETY GATE:** this scaffold cannot inspect the task text filled in above.
        | If the task will start, stop, delete, restart, profile, or otherwise drive Herdr lifecycle behavior, stop and regenerate the brief with `--herdr-lab` before dispatch.
        | Do not add Herdr lifecycle commands to this unguarded brief by hand.
    after the 'global' fill: task body appears 1 time(s)
      unguarded HARD SAFETY GATE as the worker would read it:
        | # Herdr lifecycle declaration - NOT ENABLED
        | **HARD SAFETY GATE:** this scaffold cannot inspect the task text filled in above.
        | If the task will start, stop, delete, restart, profile, or otherwise drive Herdr lifecycle behavior, stop and regenerate the brief with `--herdr-lab` before dispatch.
        | Do not add Herdr lifecycle commands to this unguarded brief by hand.

==============================================================================
BEFORE - pre-merge fork main (bin/fm-brief.sh at 1fd2fbe)
==============================================================================
  ship scaffold, UNGUARDED (no --herdr-lab)
    {TASK} fill sites in the scaffold: 2
    after the 'line' fill: task body appears 2 time(s)
      unguarded HARD SAFETY GATE as the worker would read it:
        | # Herdr lifecycle declaration - NOT ENABLED
        | **HARD SAFETY GATE:** this scaffold cannot inspect the task text that replaces `Restart the herdr session for the export lane, then profile it and report the hot path` later.
        | If the task will start, stop, delete, restart, profile, or otherwise drive Herdr lifecycle behavior, stop and regenerate the brief with `--herdr-lab` before dispatch.
        | Do not add Herdr lifecycle commands to this unguarded brief by hand.
    after the 'global' fill: task body appears 2 time(s)
      unguarded HARD SAFETY GATE as the worker would read it:
        | # Herdr lifecycle declaration - NOT ENABLED
        | **HARD SAFETY GATE:** this scaffold cannot inspect the task text that replaces `Restart the herdr session for the export lane, then profile it and report the hot path` later.
        | If the task will start, stop, delete, restart, profile, or otherwise drive Herdr lifecycle behavior, stop and regenerate the brief with `--herdr-lab` before dispatch.
        | Do not add Herdr lifecycle commands to this unguarded brief by hand.

  ship scaffold, GUARDED (--herdr-lab)
    {TASK} fill sites in the scaffold: 1
    after the 'line' fill: task body appears 1 time(s)
      guarded Herdr lifecycle contract as the worker would read it:
        | # Herdr isolation - HARD SAFETY CONTRACT
        | This brief was explicitly scaffolded with `--herdr-lab` because the task will drive Herdr lifecycle behavior.
        | On Herdr 0.7.3 the API socket is not relocatable by `HERDR_CONFIG_PATH`, `XDG_CONFIG_HOME`, or `HOME`.
        | A named non-`default` session plus a trailing `--session <name>` on every call is the only viable local isolation.
        | 1. Set `HERDR_LAB_HELPER='/tmp/fm-brief-fill.NC3fKI/bin/fm-herdr-lab.sh'` and generate the session name with `HERDR_LAB_SESSION=$("$HERDR_LAB_HELPER" name ship-guarded)`.
        |    Install `trap '"$HERDR_LAB_HELPER" teardown "$HERDR_LAB_SESSION"' EXIT` before provisioning, then provision only with `"$HERDR_LAB_HELPER" provision "$HERDR_LAB_SESSION"`.
        | 2. Run every task-specific non-lifecycle Herdr command through `"$HERDR_LAB_HELPER" run "$HERDR_LAB_SESSION" <arguments...>`.
        |    The helper appends the required trailing `--session "$HERDR_LAB_SESSION"`; `HERDR_SESSION` alone is never accepted as isolation.
    after the 'global' fill: task body appears 1 time(s)
      guarded Herdr lifecycle contract as the worker would read it:
        | # Herdr isolation - HARD SAFETY CONTRACT
        | This brief was explicitly scaffolded with `--herdr-lab` because the task will drive Herdr lifecycle behavior.
        | On Herdr 0.7.3 the API socket is not relocatable by `HERDR_CONFIG_PATH`, `XDG_CONFIG_HOME`, or `HOME`.
        | A named non-`default` session plus a trailing `--session <name>` on every call is the only viable local isolation.
        | 1. Set `HERDR_LAB_HELPER='/tmp/fm-brief-fill.NC3fKI/bin/fm-herdr-lab.sh'` and generate the session name with `HERDR_LAB_SESSION=$("$HERDR_LAB_HELPER" name ship-guarded)`.
        |    Install `trap '"$HERDR_LAB_HELPER" teardown "$HERDR_LAB_SESSION"' EXIT` before provisioning, then provision only with `"$HERDR_LAB_HELPER" provision "$HERDR_LAB_SESSION"`.
        | 2. Run every task-specific non-lifecycle Herdr command through `"$HERDR_LAB_HELPER" run "$HERDR_LAB_SESSION" <arguments...>`.
        |    The helper appends the required trailing `--session "$HERDR_LAB_SESSION"`; `HERDR_SESSION` alone is never accepted as isolation.

  scout scaffold, UNGUARDED
    {TASK} fill sites in the scaffold: 2
    after the 'line' fill: task body appears 2 time(s)
      unguarded HARD SAFETY GATE as the worker would read it:
        | # Herdr lifecycle declaration - NOT ENABLED
        | **HARD SAFETY GATE:** this scaffold cannot inspect the task text that replaces `Restart the herdr session for the export lane, then profile it and report the hot path` later.
        | If the task will start, stop, delete, restart, profile, or otherwise drive Herdr lifecycle behavior, stop and regenerate the brief with `--herdr-lab` before dispatch.
        | Do not add Herdr lifecycle commands to this unguarded brief by hand.
    after the 'global' fill: task body appears 2 time(s)
      unguarded HARD SAFETY GATE as the worker would read it:
        | # Herdr lifecycle declaration - NOT ENABLED
        | **HARD SAFETY GATE:** this scaffold cannot inspect the task text that replaces `Restart the herdr session for the export lane, then profile it and report the hot path` later.
        | If the task will start, stop, delete, restart, profile, or otherwise drive Herdr lifecycle behavior, stop and regenerate the brief with `--herdr-lab` before dispatch.
        | Do not add Herdr lifecycle commands to this unguarded brief by hand.

labs kept at /tmp/fm-brief-fill.NC3fKI
